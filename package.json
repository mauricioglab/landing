{
  "name": "mauricio-landing",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev --host",
    "start": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview --host",
    "astro": "astro",
    "deploy": "npm run build && git add . && git commit -m \"Deploy: Build automático\" && git push",
    "deploy:force": "npm run build && git add . && git commit -m \"Deploy: Build automático\" && git push --force",
    "serve": "astro preview --host",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.13.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.3.0",
    "autoprefixer": "^10.4.21",
    "typescript": "^5.0.0"
  }
}
